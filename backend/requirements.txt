pydantic==2.11.3
# pydantic-ai==0.0.19  # Optional: only needed for claude/groq/openai providers.
# The bundled gemini path uses google-genai directly. Re-enable when
# Phase 4 Task 1B brings back multi-provider support via the Settings UI.
pandas==2.2.3
devtools==0.12.2
python-dotenv==1.1.0
fastapi==0.115.9
uvicorn==0.34.0
python-multipart==0.0.20
aiosqlite==0.21.0
google-genai>=1.0.0
# Phase 6 Task 4: YouTube transcript fetcher.
# youtube-transcript-api is the fast/lightweight default; yt-dlp is
# the heavier-but-more-actively-maintained fallback used when the
# first one breaks. Pinned with >= rather than == so a backend
# restart picks up the latest published fix when YouTube rotates
# their internal API (typically 1-3 times a year).
youtube-transcript-api>=1.0.0
yt-dlp>=2025.1.0
# Phase 7 Task 1: vector similarity search inside the existing
# SQLite DB. Distributed as a loadable extension; the connection
# loads it with sqlite_vec.load(conn) before any vec_* SQL runs.
sqlite-vec>=0.1.6
# Cloud mode: cloud_forward.py uses httpx for proxy calls.
httpx>=0.25